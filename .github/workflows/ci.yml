name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package + dev deps
        # `docs` is required too: the docs-site scenarios shell out to mkdocs,
        # and skip themselves (silently reducing coverage) when it is absent.
        run: pip install -e ".[dev,docs]"
      - name: Run tests
        run: pytest -q

  security:
    name: security scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install package + scanners
        run: pip install -e ".[dev]" bandit pip-audit
      - name: Bandit (static analysis)
        continue-on-error: true
        run: bandit -r phlatline/ -ll
      - name: pip-audit (dependency CVEs)
        run: pip-audit
